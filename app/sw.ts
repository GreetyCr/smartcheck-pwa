/**
 * Service Worker (Serwist).
 * Serwist genera el SW en build; este archivo puede usarse como entrada o config.
 * Ver documentación @serwist/next para la configuración final.
 */
export default undefined;
